services:
  backend:
    # build:
    #   context: .
    #   dockerfile: backend/dockerfile
    # container_name: masking-backend
    env_file:
      - .env
    # environment:
    #   DB_URL: ${DB_URL}
    # command:
    #   ["uvicorn", "backend.main:app", "--host", "0.0.0.0", "--port", "8000"]
    image: masking-backend:1.0
    ports:
      - "8000:8000"

  frontend:
    # build:
    #   context: ./frontend
    #   dockerfile: dockerfile
    # container_name: masking-frontend
    image: masking-frontend:1.0
    depends_on:
      - backend
    ports:
      - "8501:8501"

volumes:
  pgdata:
